{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "engines": {
    "node": "16.19.0"
  },
  "scripts": {
    "sequelize": "sequelize",
    "sequelize-cli": "sequelize-cli",
    "start": "per-env",
    "start:development": "nodemon -r dotenv/config ./bin/www",
    "start:production": "node ./bin/www",
    "build": "node psql-setup-script.js",
    "db:create": "npx dotenv sequelize db:create",
    "db:drop": "npx dotenv sequelize db:drop",
    "db:migrate": "npx dotenv sequelize db:migrate",
    "db:migrate:undo:all": "npx dotenv sequelize db:migrate:undo:all",
    "db:redo": "npx dotenv sequelize db:seed:undo:all && npx dotenv sequelize db:migrate:undo:all && npx dotenv sequelize db:migrate && npx dotenv sequelize db:seed:all",
    "db:seed:all": "npx dotenv sequelize db:seed:all",
    "db:seed:undo:all": "npx sequelize-cli db:seed:undo:all",
    "migration:generate": "npx sequelize-cli migration:generate",
    "model:generate": "npx sequelize-cli model:generate",
    "seed:generate": "npx sequelize-cli seed:generate"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.7.7",
    "bcryptjs": "^2.4.3",
    "cookie-parser": "^1.4.6",
    "cors": "^2.8.5",
    "crypto": "^1.0.1",
    "csurf": "^1.11.0",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-async-errors": "^3.1.1",
    "express-async-handler": "^1.2.0",
    "express-validator": "^7.0.1",
    "faker": "^6.6.6",
    "helmet": "^7.0.0",
    "jsonwebtoken": "^9.0.2",
    "morgan": "^1.10.0",
    "nodemailer": "^6.9.7",
    "per-env": "^1.0.2",
    "pg": "^8.11.3",
    "sequelize": "^6.33.0",
    "sequelize-cli": "^6.6.1",
    "zxcvbn": "^4.4.2"
  },
  "devDependencies": {
    "@types/js-cookie": "^3.0.6",
    "dotenv-cli": "^7.3.0",
    "nodemon": "^3.0.1"
  }
}
